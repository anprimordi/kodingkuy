<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="um.informatika.kodingkuy">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".feature.about.AboutActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".feature.materi.detail.DetailMateriActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".feature.materi.MateriActivity"
            android:label="Materi"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />
        <activity android:name=".feature.kuis.KuisActivity" />
        <activity
            android:name=".feature.pendahuluan.PendahuluanActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".feature.home.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>